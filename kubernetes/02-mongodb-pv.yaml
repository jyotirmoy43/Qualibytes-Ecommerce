apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
  namespace: qbshop
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mongodb
  persistentVolumeReclaimPolicy: Retain
