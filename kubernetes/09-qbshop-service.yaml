apiVersion: v1
kind: Service
metadata:
  name: qbshop-service
  namespace: qbshop
spec:
  type: NodePort
  selector:
    app: qbshop
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
